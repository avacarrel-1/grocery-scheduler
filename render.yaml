services:
  - type: web
    name: grocery-scheduler-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URL
        value: mongodb+srv://avacarrel:PWih03O7JCO3fPze@grocery-scheduler-clust.6fxjap1.mongodb.net/?retryWrites=true&w=majority&appName=grocery-scheduler-cluster
      - key: DB_NAME
        value: grocery_scheduler
      - key: CORS_ORIGINS
        value: "*"
